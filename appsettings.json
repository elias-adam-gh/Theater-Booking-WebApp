{
  "ConnectionStrings": {
    "DefaultConnectionString": "Data Source=localhost\\SQLExpress;Initial Catalog=bookingAppDb;Integrated Security=True;Pooling=False"
    // The connection string for your SQL Server database
    // "Integrated Security=True" means Windows authentication is used

  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
      // Logging configuration for the application.
      // The default log level is set to "Information", which means logs with severity "Information" and higher will be recorded.
      // Logs generated by "Microsoft.AspNetCore" will be recorded with severity "Warning" and higher.
    }
  },
  "AllowedHosts": "*"
  // Configures the list of allowed hostnames for the application.
  // "*" allows any host to access the application.
}
